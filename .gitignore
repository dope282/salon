node_modules
.next
*.log
.vercel

# Орчны хувьсагч (нууц) — example-ийг үлдээнэ
.env*
!.env.local.example

# Байршуулсан зургууд (runtime)
/uploads/

# Supabase-аас экспортолсон дата (хувийн мэдээлэл агуулна — бүү commit)
supabase-export*.json
*-export.json
